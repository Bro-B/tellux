import{_ as s,o as i,c as a,a2 as t}from"./chunks/framework.D5wUhsuj.js";const g=JSON.parse('{"title":"实体贴地（Ground Clamp）技术方案","description":"","frontmatter":{},"headers":[],"relativePath":"design/ground-clamp.md","filePath":"design/ground-clamp.md","lastUpdated":1784538154000}'),o={name:"design/ground-clamp.md"};function d(n,e,l,r,c,h){return i(),a("div",null,[...e[0]||(e[0]=[t(`<h1 id="实体贴地-ground-clamp-技术方案" tabindex="-1">实体贴地（Ground Clamp）技术方案 <a class="header-anchor" href="#实体贴地-ground-clamp-技术方案" aria-label="Permalink to &quot;实体贴地（Ground Clamp）技术方案&quot;">​</a></h1><blockquote><p>状态：<strong>设计中，暂未实现</strong>（2026-07-01；API 于 2026-07-03 改版：§4 由 <code>HeightReference</code> 7 值枚举改为统一 <code>clamp</code> 字段） 范围：点 / 线 / 面实体的真·贴地渲染，对标 Cesium <code>GroundPrimitive</code> / <code>GroundPolylinePrimitive</code>。 渲染器：<strong>WebGL 优先全量实现</strong>；WebGPU 支持暂不处理，后续单独立项。</p></blockquote><hr><h2 id="_0-背景与决策" tabindex="-1">0. 背景与决策 <a class="header-anchor" href="#_0-背景与决策" aria-label="Permalink to &quot;0. 背景与决策&quot;">​</a></h2><h3 id="_0-1-现状" tabindex="-1">0.1 现状 <a class="header-anchor" href="#_0-1-现状" aria-label="Permalink to &quot;0.1 现状&quot;">​</a></h3><p>实体位置由 <a href="../../src/Viewer.ts">Viewer.cartographicToVector3</a> 经椭球 <code>getCartographicToPosition</code> 落点，输入里的 <code>height</code> 米数即最终绝对椭球高，<strong>无任何地形跟随</strong>。材质统一 <code>depthWrite: false</code>（<a href="../../src/entities/PolygonGraphic.ts">PolygonGraphic</a>、<a href="../../src/entities/PolylineGraphic.ts">PolylineGraphic</a>、<a href="../../src/entities/EntityRenderManager.ts">EntityRenderManager</a>）。</p><h3 id="_0-2-被否决的方案-cpu-采样重建" tabindex="-1">0.2 被否决的方案：CPU 采样重建 <a class="header-anchor" href="#_0-2-被否决的方案-cpu-采样重建" aria-label="Permalink to &quot;0.2 被否决的方案：CPU 采样重建&quot;">​</a></h3><p>&quot;沿几何采样地形高程 → 重建几何&quot;的妥协路线被否决，原因：</p><ul><li>terrain/3D Tiles 是流式 LOD，相机移动、新瓦片载入后采样即过时，需要反复重采样重建，面几何每次重建代价高；</li><li>仍会出现穿山 / 浮空，<strong>满足不了真贴地需求</strong>。</li></ul><h3 id="_0-3-采用的方案-gpu-阴影体-深度纹理逐片元分类" tabindex="-1">0.3 采用的方案：GPU 阴影体 + 深度纹理逐片元分类 <a class="header-anchor" href="#_0-3-采用的方案-gpu-阴影体-深度纹理逐片元分类" aria-label="Permalink to &quot;0.3 采用的方案：GPU 阴影体 + 深度纹理逐片元分类&quot;">​</a></h3><p>完全对标 Cesium：<strong>CPU 建静态&quot;分类体（shadow volume）&quot;几何，渲染时在片元着色器里读主场景深度纹理、还原地表点、逐像素判定该点是否落在图元 footprint 内</strong>。几何不随地形 LOD 重建。</p><p>该方案在 tellux <strong>可行且不从零搭</strong>，因为：</p><table tabindex="0"><thead><tr><th>Cesium 依赖</th><th>tellux 现状</th></tr></thead><tbody><tr><td><code>czm_globeDepthTexture</code>（地形+瓦片深度）</td><td><code>postprocessing</code> EffectComposer 的 <code>readBuffer.depthTexture</code>，主场景（terrain + 3D Tiles + 不透明实体）已渲入其中</td></tr><tr><td>片元读深度 + 改写材质注入分类逻辑</td><td><a href="../../src/entities/EntityRenderManager.ts">EntityRenderManager</a> 的 OIT pass 已用 <code>onBeforeCompile</code> + <code>telluxSceneDepth</code> uniform 读 <code>readBuffer.depthTexture</code> 做 <code>telluxDepthDiscard</code>（L336-357）</td></tr><tr><td>多 pass 编排</td><td>effects 链 +<code>EntityRenderManager</code> 已是多 pass 范式</td></tr><tr><td>tile 材质打 stencil bit</td><td>已有 tile 材质插件机制（<code>TilesetModelPlugins</code>、<code>WebGPUTerrainOverlayPlugin</code>）可 patch 材质</td></tr></tbody></table><p>即：<strong>贴地分类 pass 与 OIT pass 同构</strong>——读 <code>readBuffer.depthTexture</code>、用自定义材质渲一组几何、把分类结果合成回主色。</p><h3 id="_0-4-关键约束" tabindex="-1">0.4 关键约束 <a class="header-anchor" href="#_0-4-关键约束" aria-label="Permalink to &quot;0.4 关键约束&quot;">​</a></h3><ul><li><strong>WebGPU 下 <code>onBeforeCompile</code> GLSL 注入失效</strong>（已记录坑点）。本方案的材质注入强依赖该机制，故 <strong>WebGL 优先</strong>；WebGPU 需改用 TSL/WGSL 原生着色器，<strong>本期不做</strong>。</li><li>点实体走 <strong>CPU <code>clamp</code> 采样</strong>（见 §3.7）。这是 Cesium 对点 / 布告板 / 标签 / 模型的<strong>正解</strong>（<code>Model.updateClamping</code>、<code>Billboard._updateClamping</code> 均 CPU 采样改 modelMatrix），不是妥协。线 / 面才用 GPU 分类。</li></ul><hr><h2 id="_1-cesium-实现回顾-决策依据" tabindex="-1">1. Cesium 实现回顾（决策依据） <a class="header-anchor" href="#_1-cesium-实现回顾-决策依据" aria-label="Permalink to &quot;1. Cesium 实现回顾（决策依据）&quot;">​</a></h2><blockquote><p>源码版本 1.136，路径 <code>D:/dev_work/gis-template/node_modules/cesium/Build/CesiumUnminified/</code>。</p></blockquote><h3 id="_1-1-贴地线-静态墙体积-逐片元分类" tabindex="-1">1.1 贴地线：静态墙体积 + 逐片元分类 <a class="header-anchor" href="#_1-1-贴地线-静态墙体积-逐片元分类" aria-label="Permalink to &quot;1.1 贴地线：静态墙体积 + 逐片元分类&quot;">​</a></h3><ul><li><strong>几何</strong>（<code>Workers/createGroundPolylineGeometry.js</code>）：沿大地线 / 恒向线按 <code>granularity</code>（默认 9999m）细分，<strong>不采样地形</strong>；每段生成一个 <code>minHeight..maxHeight</code>（0..1000m）的墙立方体（8 顶点 36 索引，<code>REFERENCE_INDICES</code> L1075）。<code>min/maxHeight</code> 仅来自超粗全局表 <code>ApproximateTerrainHeights</code>（6 级 ≈1°），<strong>只用于撑大包围球、保证墙体包住地形范围</strong>（L1434），不参与真实贴合。</li><li><strong>顶点属性</strong>：<code>startHi/startLo</code>（<code>EncodedCartesian3</code> 高 / 低双精度拆分）、<code>forwardOffset</code>、<code>startPlaneNormal</code>、<code>endPlaneNormal</code>、<code>rightNormal</code>、texcoord 归一化（L1130-1558）。</li><li><strong>渲染</strong>（<code>Cesium.js</code> <code>GroundPolylinePrimitive</code> / <code>PolylineShadowVolumeFS</code> L57231，pass <code>TERRAIN_CLASSIFICATION</code>=3）：<div class="language-glsl vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">glsl</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">float</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> d </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> czm_unpackDepth</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">texture</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(czm_globeDepthTexture, gl_FragCoord.xy </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> czm_viewport.zw));</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (d </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">==</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">discard</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                       // 天空丢弃</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">vec4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> eye </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> czm_windowToEyeCoordinates</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(gl_FragCoord.xy, d);</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 用当前线段的 右平面 / 起点平面 / 终点平面 + 半宽 判定地表点是否在线条 footprint 内</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">abs</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">planeDistance</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(v_rightPlaneEC, eye.xyz)) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> halfWidth) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">discard</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">planeDistance</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(v_startPlane..., eye.xyz) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">discard</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">planeDistance</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(v_endPlane...,   eye.xyz) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&lt;</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 0.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">discard</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// 命中 → 输出颜色，czm_depthClamp() 让墙体在地下也参与</span></span></code></pre></div></li><li><code>czm_depthClamp</code> 保证墙体被地形遮挡时片元仍参与计算；墙体几何只提供&quot;哪些像素运行 FS&quot;+&quot;平面属性&quot;，可见结果完全由重建的地表点决定。</li></ul><h3 id="_1-2-贴地面-模板阴影体两遍" tabindex="-1">1.2 贴地面：模板阴影体两遍 <a class="header-anchor" href="#_1-2-贴地面-模板阴影体两遍" aria-label="Permalink to &quot;1.2 贴地面：模板阴影体两遍&quot;">​</a></h3><p><code>GroundPrimitive</code>（= <code>_extruded:true</code> 的 <code>ClassificationPrimitive</code>，L54413）每几何<strong>两条命令</strong>：</p><table tabindex="0"><thead><tr><th>pass</th><th>colorMask</th><th>模板</th><th>作用</th></tr></thead><tbody><tr><td>stencil-depth（L53611）</td><td>全 false</td><td><code>zFail: 前 DECREMENT_WRAP / 后 INCREMENT_WRAP</code></td><td>阴影体打掩码：地形挡住墙体处掩码 ≠0 = &quot;在体内且可见&quot;</td></tr><tr><td>color（L53645）</td><td>写色</td><td><code>NOT_EQUAL 0</code>，命中后 ops 全 <code>ZERO</code> 清掩码</td><td>只给掩码 ≠0 的像素着色</td></tr></tbody></table><p>color pass 的 <code>ShadowVolumeAppearanceFS</code>（L52658）同样读深度还原世界坐标，并用 <code>vectorFromOffset</code> <strong>采样相邻像素深度重建地表法线</strong>用于打光。</p><h3 id="_1-3-3d-tiles-分类-同几何-换-pass-模板-深度源" tabindex="-1">1.3 3D Tiles 分类：同几何 + 换 pass / 模板 / 深度源 <a class="header-anchor" href="#_1-3-3d-tiles-分类-同几何-换-pass-模板-深度源" aria-label="Permalink to &quot;1.3 3D Tiles 分类：同几何 + 换 pass / 模板 / 深度源&quot;">​</a></h3><p>3D Tiles 画时 <code>REPLACE</code> 写模板位 128（<code>CESIUM_3D_TILE_MASK</code>），分类命令在 <code>CESIUM_3D_TILE_CLASSIFICATION</code>（pass=6）用 <code>EQUAL</code> 模板门控、深度源换为瓦片打包深度。<strong>同一份分类体，仅换 pass + 渲染状态 + 深度来源</strong>——对应 tellux <code>HeightSampler.source</code> 的 <code>terrain | tileset | all</code>。</p><h3 id="_1-4-heightreference-cpu-采样-点-布告板-模型" tabindex="-1">1.4 heightReference：CPU 采样（点 / 布告板 / 模型） <a class="header-anchor" href="#_1-4-heightreference-cpu-采样-点-布告板-模型" aria-label="Permalink to &quot;1.4 heightReference：CPU 采样（点 / 布告板 / 模型）&quot;">​</a></h3><p><code>CLAMP_TO_GROUND</code> = 清零高度偏移；<code>RELATIVE_TO_GROUND</code> = 采样地表高 + 偏移。CPU 改 modelMatrix / 位置属性，<strong>与 GroundPrimitive 着色器无关</strong>。</p><h3 id="_1-5-两个值得落实的工程细节" tabindex="-1">1.5 两个值得落实的工程细节 <a class="header-anchor" href="#_1-5-两个值得落实的工程细节" aria-label="Permalink to &quot;1.5 两个值得落实的工程细节&quot;">​</a></h3><ol><li><strong><code>EncodedCartesian3</code> 高 / 低双精度</strong>：顶点位置拆 <code>high + low</code>（Float32 + Float32），VS 里 <code>vec3 p = high.xyz + low.xyz</code> 还原，对抗地球尺度 Float32 抖动。当前 <a href="../../src/entities/PolylineGraphic.ts">PolylineGraphic</a> 用 <code>LineGeometry.setPositions</code>（Float32），长贴地线必抖。</li><li><strong>法向微偏移（normal nudge）治 z-fighting</strong>：墙体前 / 后两面沿 <code>rightNormal</code> 各偏移 <code>±EPSILON5</code>（L1458-1489）+ <code>nudgeXZ</code>（L1014）处理日期变更线，而非简单抬高度（抬高度会破坏贴地语义）。</li></ol><hr><h2 id="_2-总体架构" tabindex="-1">2. 总体架构 <a class="header-anchor" href="#_2-总体架构" aria-label="Permalink to &quot;2. 总体架构&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>postprocessing EffectComposer 链</span></span>
<span class="line"><span>  ├─ [主场景渲染]  terrain + 3D Tiles + 不透明实体 → readBuffer（含 depthTexture）</span></span>
<span class="line"><span>  ├─ EntityRenderManager（OIT，已有）</span></span>
<span class="line"><span>  ├─ GroundClampPass（新增） ─────────────────────────────┐</span></span>
<span class="line"><span>  │     in:  readBuffer.texture（主色） + readBuffer.depthTexture（地表深度）</span></span>
<span class="line"><span>  │     渲:  groundClampRoot（贴地线墙体积 + 贴地面阴影体，分类材质）</span></span>
<span class="line"><span>  │     出:  writeBuffer（主色 ⊕ 分类着色）</span></span>
<span class="line"><span>  ├─ 云 / 大气 EffectPass（可能 swap；之后的 readBuffer 可能无深度）</span></span>
<span class="line"><span>  ├─ EntityRenderManager（透明实体 OIT，位于大气之后，就地叠加）</span></span>
<span class="line"><span>  ├─ SymbolOcclusionPass</span></span>
<span class="line"><span>  └─ LensFlare → SMAA → Dithering</span></span></code></pre></div><p>数据流（分类片元）：</p><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>gl_FragCoord → 采样 depthTexture → 窗口深度 z</span></span>
<span class="line"><span>  → NDC → clip → inverseProjection → 眼坐标 eye</span></span>
<span class="line"><span>  → (inverseView → 世界坐标 world，面/打光用)</span></span>
<span class="line"><span>  → 平面测试（线：右/起/终平面 + 半宽；面：阴影体模板已界定内外）</span></span>
<span class="line"><span>  → 命中：输出颜色（预乘 alpha）；未命中：discard</span></span></code></pre></div><p>新增组件：</p><table tabindex="0"><thead><tr><th>组件</th><th>职责</th><th>对标 Cesium</th></tr></thead><tbody><tr><td><code>GroundClampPass</code></td><td>effects pass：绑定深度、渲分类几何、合成</td><td><code>TERRAIN_CLASSIFICATION</code> pass 编排</td></tr><tr><td><code>GroundPolylineGraphic</code></td><td>线墙体积几何 + 分类材质</td><td><code>GroundPolylineGeometry</code> + <code>PolylineShadowVolumeFS</code></td></tr><tr><td><code>GroundPolygonGraphic</code></td><td>面阴影体几何 + 模板两遍材质</td><td><code>PolygonGeometry.createShadowVolume</code> + <code>ShadowVolumeAppearanceFS</code></td></tr><tr><td><code>EncodedCartesian3</code>（util）</td><td>高 / 低双精度拆分</td><td><code>EncodedCartesian3</code></td></tr><tr><td><code>clamp</code> 字段 + 解析</td><td>API（统一 <code>boolean | GroundClamp</code>）</td><td>取代 <code>HeightReference</code> 7 值枚举</td></tr><tr><td>点：复用<code>HeightSampler</code></td><td>CPU 采样改点位置</td><td><code>Billboard._updateClamping</code></td></tr></tbody></table><hr><h2 id="_3-组件设计" tabindex="-1">3. 组件设计 <a class="header-anchor" href="#_3-组件设计" aria-label="Permalink to &quot;3. 组件设计&quot;">​</a></h2><h3 id="_3-1-深度源与坐标还原" tabindex="-1">3.1 深度源与坐标还原 <a class="header-anchor" href="#_3-1-深度源与坐标还原" aria-label="Permalink to &quot;3.1 深度源与坐标还原&quot;">​</a></h3><ul><li><p><strong>深度源</strong>：贴地分类位于大气之前，复用主场景的 <code>readBuffer.depthTexture</code>。clamped 实体 <code>depthWrite:false</code>，不污染地表深度；该纹理所含即 terrain + 3D Tiles 深度（Phase 1 = union）。</p></li><li><p><strong>还原</strong>（ShaderMaterial 内置 <code>projectionMatrix</code> / <code>viewMatrix</code>）：</p><div class="language-glsl vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">glsl</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">float</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> z </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> texture2D</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(telluxGroundDepth, gl_FragCoord.xy </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> uResolution).r;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  // [0,1] 窗口深度</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">if</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (z </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">&gt;=</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">discard</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                                      // 天空（需按实际 clear 深度核实）</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">vec4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> clip </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> vec4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">vec3</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(gl_FragCoord.xy </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> uResolution, z) </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 2.0</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> -</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> 1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">, </span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">1.0</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">);</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">vec4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> eye </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> inverseProjection </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> clip; eye </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">/=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> eye.w;</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">vec4</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> world </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> inverseView </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> eye;</span><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">                                            // 面片元打光用</span></span></code></pre></div><p>深度纹理类型 / 编码需与投影一致，验证 <code>readBuffer.depthTexture</code> 的 <code>type</code>（fixed vs HalfFloat）与是否启用 logarithmic depth。</p></li></ul><h3 id="_3-2-贴地线几何-port-creategroundpolylinegeometry" tabindex="-1">3.2 贴地线几何（port <code>createGroundPolylineGeometry</code>） <a class="header-anchor" href="#_3-2-贴地线几何-port-creategroundpolylinegeometry" aria-label="Permalink to &quot;3.2 贴地线几何（port \`createGroundPolylineGeometry\`）&quot;">​</a></h3><ul><li>沿椭球细分：大地线（<code>EllipsoidGeodesic</code>）/ 恒向线（<code>EllipsoidRhumbLine</code>），<code>granularity</code> 默认 9999m。tellux 可用现有椭球（<code>tilesets.tileset.ellipsoid</code>）。</li><li>每段墙体积：8 顶点 / 36 索引（直引 <code>REFERENCE_INDICES</code>），<code>min/maxHeight</code> 撑包络。</li><li><code>min/maxHeight</code> 来源：<strong>Phase 1 用固定带 + <code>HeightSampler</code> 粗采样</strong>（对标 <code>ApproximateTerrainHeights</code>），仅作包围球与墙体高度，不参与贴合判定。</li><li>顶点属性 port：<code>startHi/startLo</code>、<code>forwardOffset</code>、<code>start/end/right</code> 平面法向、texcoord 归一化（含 2D 属性可裁剪，tellux 仅 3D 模式）。</li><li><strong>细节 1 落实</strong>：<code>startHi/startLo</code> 经 <code>EncodedCartesian3.fromCartesian</code> 拆分。</li><li><strong>细节 2 落实</strong>：前 / 后两面沿 <code>rightNormal</code> 各 <code>±EPSILON5</code> 偏移 + <code>nudgeXZ</code>。</li></ul><h3 id="_3-3-贴地线分类材质-port-polylineshadowvolumefs-vs" tabindex="-1">3.3 贴地线分类材质（port <code>PolylineShadowVolumeFS/VS</code>） <a class="header-anchor" href="#_3-3-贴地线分类材质-port-polylineshadowvolumefs-vs" aria-label="Permalink to &quot;3.3 贴地线分类材质（port \`PolylineShadowVolumeFS/VS\`）&quot;">​</a></h3><ul><li><strong>VS</strong>：用 <code>startHi + startLo</code> 双精度还原段起点；由属性 + <code>modelViewMatrix</code> 算眼空间 <code>rightPlaneEC / startPlaneEC / endPlaneEC</code> 与 <code>halfWidth</code> 作 varying。</li><li><strong>FS</strong>：§3.1 还原地表 <code>eye</code>；<code>abs(planeDistance(rightPlane, eye)) &lt;= halfWidth &amp;&amp; planeDistance(startPlane) &gt;= 0 &amp;&amp; planeDistance(endPlane) &gt;= 0</code> 否则 discard；命中输出预乘 alpha 颜色。</li><li><strong>渲染状态</strong>：<code>depthTest:false</code>（手动比深度）、<code>depthWrite:false</code>、<code>blending: Normal</code> 或预乘 alpha；<strong>不需 stencil</strong>（单 pass 深度纹理分类，比 Cesium 更简，因始终有深度纹理）。</li></ul><h3 id="_3-4-贴地面几何-每三角形棱柱" tabindex="-1">3.4 贴地面几何（每三角形棱柱） <a class="header-anchor" href="#_3-4-贴地面几何-每三角形棱柱" aria-label="Permalink to &quot;3.4 贴地面几何（每三角形棱柱）&quot;">​</a></h3><p><strong>已实现（P1，2026-07-03），与原设想的整体阴影体不同</strong>：外环投影到质心切平面，<code>THREE.ShapeUtils.triangulateShape</code>（earcut）三角化——凹多边形由三角化天然解决；<strong>每个三角形</strong>沿逐顶点椭球法向挤出一个棱柱分类体（6 顶点 / 8 三角形，绕序统一朝外），高度带与线一致（固定 [-1000, 9000] m，不采样地形）。相邻三角形共享顶点与法向，侧面严格贴合。见 <a href="../../src/entities/GroundPolygonGraphic.ts">GroundPolygonGraphic</a>。</p><h3 id="_3-5-贴地面分类材质-单-pass-半平面测试-非模板两遍" tabindex="-1">3.5 贴地面分类材质（单 pass 半平面测试，非模板两遍） <a class="header-anchor" href="#_3-5-贴地面分类材质-单-pass-半平面测试-非模板两遍" aria-label="Permalink to &quot;3.5 贴地面分类材质（单 pass 半平面测试，非模板两遍）&quot;">​</a></h3><p><strong>原方案（Cesium 模板两遍阴影体）被否决</strong>：P0 核实内置合成器的 render target <code>stencilBuffer:false</code>（renderer 未开 stencil），走模板需自管 RT + 深度共享，复杂度不成比例。采用原&quot;备选简化&quot;路线并解决其凹多边形难点：</p><ul><li>凹多边形 point-in-polygon 不在片元里做——CPU earcut 后每片元只需判定<strong>所在三角形棱柱</strong>：FS 重建眼空间地表点，做三条边的外向半平面测试（<code>cross(边, up)</code>），全部内侧即命中。</li><li><strong>全多边形共用一个 up 向量</strong>（质心方向，uniform）：共享边在相邻三角形里方向恰好相反，叉积精确反号 → 两侧测试严格互补，无缝隙无重叠。</li><li><strong><code>side: BackSide</code></strong>（凸体每像素恰好一次 FS，相机在体内也正确）→ 支持半透明填充不重复混合；填充色的 <code>rgba(...)</code> / <code>#rrggbbaa</code> alpha 生效（线同步改为 BackSide）。</li><li>渲染状态与线一致：<code>depthTest:false</code>、<code>depthWrite:false</code>、常规混合，由 <code>GroundClampPass</code> 统一渲染，无 stencil 依赖。<code>extrudeHeight</code> / <code>outline</code> 留后续阶段。</li></ul><h3 id="_3-6-groundclamppass-编排" tabindex="-1">3.6 GroundClampPass 编排 <a class="header-anchor" href="#_3-6-groundclamppass-编排" aria-label="Permalink to &quot;3.6 GroundClampPass 编排&quot;">​</a></h3><ul><li>实现 <code>ThreeEffectPass</code>（同 <code>EntityRenderManager</code> 形态）：<code>render(renderer, writeBuffer, readBuffer)</code>。</li><li>维护 <code>groundClampRoot: THREE.Group</code>，挂所有 <code>GroundPolylineGraphic</code> / <code>GroundPolygonGraphic</code> 的 <code>object3D</code>。</li><li>绑定 <code>readBuffer.depthTexture</code> 到分类材质的 <code>telluxGroundDepth</code> uniform（同 OIT 的 <code>telluxSceneDepth</code> 注入范式）。</li><li>渲分类几何到 <code>writeBuffer</code>（主色 ⊕ 分类色）；<code>needsSwap = true</code>。</li><li><strong>插入位置</strong>：effects 链中、<code>EntityRenderManager</code> 邻近，但必须在云 / 大气合成之前。真实顺序为 <code>GroundClamp → Atmosphere → Entity OIT</code>。透明实体必须在大气之后，否则大气天空分支会抹掉其地平线像素。</li><li><strong>大气之后的深度规则</strong>：大气 pass 可能 swap 到无深度的 targetB。因此 <code>EntityRenderManager</code> 及其他需要场景深度的后处理 pass 必须从 <code>readBuffer.depthTexture ?? writeBuffer.depthTexture</code> 两侧探测，不能只读 <code>readBuffer</code>。</li></ul><h3 id="_3-7-点-clamp-cpu-采样-正解-非妥协" tabindex="-1">3.7 点：clamp CPU 采样（正解，非妥协） <a class="header-anchor" href="#_3-7-点-clamp-cpu-采样-正解-非妥协" aria-label="Permalink to &quot;3.7 点：clamp CPU 采样（正解，非妥协）&quot;">​</a></h3><ul><li><code>clamp</code> 存在（<code>true</code> 或对象）时，<code>Entity</code> add 发起 <code>HeightSampler.sampleHeightMostDetailed([pos], { source })</code>，resolve 后 <code>PointGraphic.setPosition</code>。</li><li>add 立即用椭球高先摆出来，采样 resolve 后 snap（渐进式，同瓦片流式体验）。</li><li><strong>LOD 重采样</strong>：挂 <code>TilesetSamplingAdapter</code> readiness 信号或 <code>update()</code> 周期去抖重采样（点仅 1 顶点，几乎免费）。</li><li><code>source</code> 直接取自 <code>clamp.source</code>（缺省 <code>&#39;all&#39;</code>）；最终点位 = 采样高程 + <code>clamp.offset</code>（缺省 0）。</li></ul><h3 id="_3-8-实体集成" tabindex="-1">3.8 实体集成 <a class="header-anchor" href="#_3-8-实体集成" aria-label="Permalink to &quot;3.8 实体集成&quot;">​</a></h3><ul><li><code>Entity</code> 构造：<code>polyline.clamp</code> 存在 → 建 <code>GroundPolylineGraphic</code>，加入 <code>groundClampRoot</code>（非普通 entity root）；面同理。</li><li><code>EntityManager</code> 需持有 <code>groundClampRoot</code> 引用（由 <code>GroundClampPass</code> 提供 / 注入）。</li><li>普通拾取（<a href="../../src/sampling/EntityPicker.ts">EntityPicker</a>）对线 / 面走射线或屏幕投影——贴地几何为墙体积，射线命中语义需重新定义（建议按 footprint 投影拾取，复用现有 <code>forEachSegment</code> 屏幕投影逻辑）。</li></ul><hr><h2 id="_4-api-设计" tabindex="-1">4. API 设计 <a class="header-anchor" href="#_4-api-设计" aria-label="Permalink to &quot;4. API 设计&quot;">​</a></h2><p>贴地本质是坐标的<strong>垂直定位</strong>属性，不是图形样式。它只有两个正交轴：贴到哪个面（深度源）、离那个面多高（偏移）。故<strong>不采用 Cesium 的 7 值枚举</strong>（笛卡尔积爆炸、<code>height</code> 语义被枚举远程改写、点 / 线 / 面三套 API 各行其是），改用一个<strong>统一 <code>clamp</code> 字段</strong>：<code>boolean</code> 走常见场景，对象走精细控制。</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">export</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> interface</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> GroundClamp</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** 贴到什么面；直接透传给 HeightSampler.source。默认 &#39;all&#39;（terrain 与 3D Tiles 取上）。 */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  source</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;all&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;terrain&#39;</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> |</span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;"> &#39;tileset&#39;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">  /** 地表之上的偏移（米）。0 或缺省 = 真·贴地；&gt; 0 = 抬离地表。 */</span></span>
<span class="line"><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">  offset</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;"> number</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><ul><li>落到 <code>PointOptions / PolylineOptions / PolygonOptions</code>，三者<strong>共用同一字段</strong>：<div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">clamp</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">?:</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> boolean </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">|</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> GroundClamp</span></span></code></pre></div></li><li>取值语义： <ul><li>缺省 / <code>false</code> → 绝对椭球高（当前行为，替代旧 <code>NONE</code>）。</li><li><code>true</code> → <code>{ source: &#39;all&#39;, offset: 0 }</code>，贴地，覆盖最常见诉求。</li><li>对象 → 精细控制，如 <code>{ source: &#39;terrain&#39;, offset: 5 }</code> = 地形上方 5 米。</li></ul></li><li><strong>clamp 即 offset=0 的 relative</strong>：Cesium 的 <code>CLAMP_TO_*</code> 与 <code>RELATIVE_TO_*</code> 在此塌缩为 <code>offset</code> 一个数字，不需两套枚举族；<code>source</code> 轴与 <a href="../../src/sampling/HeightSampler.ts">HeightSampler</a> 的 <code>SampleHeightOptions.source</code> 一一对应，不重复编码。</li><li>内部分发对用户透明：<code>source</code> 决定深度源（点采样）/ 分类深度快照（线 / 面）；点走 CPU 采样、线 / 面走分类渲染，用户侧始终是同一字段。</li></ul><h3 id="_4-1-实现断崖-offset-0-与-offset-0-线-面" tabindex="-1">4.1 实现断崖：offset=0 与 offset&gt;0（线 / 面） <a class="header-anchor" href="#_4-1-实现断崖-offset-0-与-offset-0-线-面" aria-label="Permalink to &quot;4.1 实现断崖：offset=0 与 offset&gt;0（线 / 面）&quot;">​</a></h3><p>概念连续，实现不连续，必须显式处理：</p><table tabindex="0"><thead><tr><th>offset</th><th>点</th><th>线 / 面</th></tr></thead><tbody><tr><td><code>0</code>（真贴地）</td><td>CPU 采样改点位</td><td><strong>GPU 影体分类</strong>（逐像素，不重建几何）</td></tr><tr><td><code>&gt; 0</code>（抬离）</td><td>CPU 采样 + 偏移</td><td>CPU 重采样抬升几何（采样间浮沉，后续阶段）</td></tr></tbody></table><ul><li>点的 offset 天然是「采样高程 + offset」，无断崖。</li><li>线 / 面 Phase 1 只实现 <code>offset=0</code>（GPU 分类）；对「线 / 面 + offset&gt;0」未实现组合 <strong><code>console.warn</code> 降级</strong>，不抛异常。</li></ul><h3 id="_4-2-polygon-的-height-extrudeheight-交互" tabindex="-1">4.2 polygon 的 height / extrudeHeight 交互 <a class="header-anchor" href="#_4-2-polygon-的-height-extrudeheight-交互" aria-label="Permalink to &quot;4.2 polygon 的 height / extrudeHeight 交互&quot;">​</a></h3><p>避免「字段被远程改写」：</p><ul><li><code>clamp</code> 存在 → 底面 = 贴地面，<code>height</code>（绝对底高）被忽略；</li><li><code>extrudeHeight</code> 重定义为<strong>相对底面的厚度</strong>（米），而非绝对顶高。</li></ul><p>即「贴地 + 向上拉伸体块」是自然组合，比 Cesium <code>GroundPrimitive</code> 只能压平更灵活。</p><h3 id="_4-3-分期" tabindex="-1">4.3 分期 <a class="header-anchor" href="#_4-3-分期" aria-label="Permalink to &quot;4.3 分期&quot;">​</a></h3><ul><li><strong>Phase 1 仅实现 <code>clamp: true</code> / <code>{ source: &#39;all&#39;, offset: 0 }</code></strong>（线 / 面真贴地 + 点采样）。</li><li><code>source: &#39;terrain&#39; | &#39;tileset&#39;</code> 依赖 §3.6 的分离深度快照（Phase 3）。</li><li><code>offset &gt; 0</code>（RELATIVE 语义）为 Phase 4。</li><li>类型可<strong>一次性声明完整</strong>（API 稳定），运行时对未实现取值 warn 降级到最近的已实现档。</li></ul><hr><h2 id="_5-分阶段计划-仅-webgl" tabindex="-1">5. 分阶段计划（仅 WebGL） <a class="header-anchor" href="#_5-分阶段计划-仅-webgl" aria-label="Permalink to &quot;5. 分阶段计划（仅 WebGL）&quot;">​</a></h2><table tabindex="0"><thead><tr><th>阶段</th><th>交付</th><th>验收</th></tr></thead><tbody><tr><td><strong>P0</strong> 深度分类管线 + 贴地线</td><td><code>GroundClampPass</code> 骨架、深度绑定、<code>EncodedCartesian3</code>、port <code>GroundPolylineGeometry</code> 墙体积、线分类材质、normal nudge、<code>clamp: true</code>（线，offset=0）</td><td>线在山地上随地形起伏贴合，相机 / LOD 变化无穿山 / 浮空、无抖动</td></tr><tr><td><strong>P1</strong> 贴地面（已实现）</td><td><code>GroundPolygonGraphic</code>：earcut 三角化 + 每三角形棱柱 + 单 pass 半平面分类（§3.4/3.5，非模板两遍）、半透明填充、<code>clamp: true</code>（面，offset=0）</td><td>面贴合地形，凹多边形正确，相邻面无 z-fighting</td></tr><tr><td><strong>P2</strong> 点 clamp</td><td><code>HeightSampler</code> 接入 + LOD 重采样、<code>clamp: true</code>（点）</td><td>点 snap 到地表，LOD 载入后位置收敛</td></tr><tr><td><strong>P3</strong> terrain/tileset 分离</td><td>主渲染中插入 terrain-only 深度快照 → <code>clamp.source: &#39;terrain&#39; | &#39;tileset&#39;</code></td><td>同一线可仅贴地形或仅贴建筑顶</td></tr><tr><td><strong>P4</strong> offset&gt;0 + 打磨</td><td><code>clamp.offset &gt; 0</code>（relative 语义）、精度 / 性能（scissor、bounding）、与 OIT 交互厘清</td><td>偏移贴地稳定，大场景帧率达标</td></tr><tr><td><s>P5 WebGPU</s></td><td><strong>本期不做</strong>，后续单独立项（TSL/WGSL 原生着色器）</td><td>—</td></tr></tbody></table><p>细节 1（双精度）与细节 2（微偏移）随 <strong>P0</strong> 落实，贯穿后续。</p><hr><h2 id="_6-风险与开放问题" tabindex="-1">6. 风险与开放问题 <a class="header-anchor" href="#_6-风险与开放问题" aria-label="Permalink to &quot;6. 风险与开放问题&quot;">​</a></h2><ul><li><strong>深度源完整性</strong>：<code>readBuffer.depthTexture</code> 是否含 3D Tiles 深度？需核实 <a href="../../src/rendering/ViewerRenderLoop.ts">ViewerRenderLoop</a> 与 <code>3d-tiles-renderer</code> 渲染时序；若 3D Tiles 在独立 pass，Phase 1 的 union 深度需调整。</li><li><strong>深度纹理格式 / log-depth</strong>：还原公式依赖深度编码；若管线启用 logarithmic depth，需走对应反演路径（对标 <code>czm_reverseLogDepth</code>）。</li><li><strong>MSAA</strong>：分类 pass 读深度需与主场景 MSAA 解析一致；postprocessing 链通常非 MSAA，需确认。</li><li><strong>模板可用性</strong>：composer 目标需 <code>stencilBuffer:true</code>，否则面模板两遍需独立 stencil RT。</li><li><strong>性能</strong>：全屏深度采样按分类片元计；用紧致包围球 + <code>renderer.setScissor</code> 裁剪到图元屏区缓解。</li><li><strong>与 OIT 交互</strong>：贴地实体通常不透明；若半透明，分类色与 OIT 累加的先后 / 混合需厘清。</li><li><strong>拾取语义</strong>：墙体积被射线命中 ≠ 用户意图；需按 footprint 重定义（§3.8）。</li><li><strong>granularity / 墙高默认值</strong>：tellux 场景尺度下需标定（Cesium 9999m / 0..1000m）。</li></ul><hr><h2 id="_7-参考" tabindex="-1">7. 参考 <a class="header-anchor" href="#_7-参考" aria-label="Permalink to &quot;7. 参考&quot;">​</a></h2><p><strong>Cesium 1.136</strong>（<code>D:/dev_work/gis-template/node_modules/cesium/Build/CesiumUnminified/</code>）：</p><ul><li><code>Workers/createGroundPolylineGeometry.js</code> — 线墙体积几何</li><li><code>Cesium.js</code> L52658 <code>ShadowVolumeAppearanceFS</code>、L57231 <code>PolylineShadowVolumeFS</code>、L53611/53645 模板 / color render state、L36379 <code>windowToEyeCoordinates</code>、L54413 <code>GroundPrimitive</code>、L57964 <code>GroundPolylinePrimitive.createCommands</code></li></ul><p><strong>tellux</strong>：</p><ul><li><a href="../../src/entities/EntityRenderManager.ts">EntityRenderManager.ts</a> — OIT depth-reading pass 范式（<code>telluxDepthDiscard</code> L351、材质注入 L336）</li><li><a href="../../src/effects.ts">effects.ts</a> — <code>ThreeEffectPass</code> / depthTexture 注入</li><li><a href="../../src/sampling/HeightSampler.ts">HeightSampler.ts</a> — 点 clamp 采样源（<code>clamp.source</code> ↔ <code>SampleHeightOptions.source</code>）</li><li><a href="../../src/sampling/EntityPicker.ts">EntityPicker.ts</a> — 拾取</li><li><a href="../../src/entities/PolylineGraphic.ts">PolylineGraphic.ts</a> / <a href="../../src/entities/PolygonGraphic.ts">PolygonGraphic.ts</a> / <a href="../../src/entities/PointGraphic.ts">PointGraphic.ts</a> — 现有 graphic</li><li><a href="../../src/types/entities.ts">entities.ts</a> — <code>EntityOptions</code> / <code>*Options</code> 类型</li></ul>`,85)])])}const k=s(o,[["render",d]]);export{g as __pageData,k as default};
